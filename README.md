# Simple Interest Calculator

A simple bash script project that calculates simple interest based on principal amount, annual rate of interest, and time period in years.

## Features
* Interactive command-line interface
* Accepts user input for principal, rate, and time
* Handles decimal values precisely using `bc`

## Technologies Used
* Bash shell scripting
* Linux/Terminal utilities
